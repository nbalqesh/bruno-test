info:
  name: GetSinglePost
  type: http
  seq: 2

http:
  method: GET
  url: https://jsonplaceholder.typicode.com/posts/1
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status should be 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("Post ID should be 1", function() {
          expect(res.body.id).to.equal(1);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
